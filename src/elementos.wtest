import elementos.*


test "alturaCastillo" {
	assert.equals(20, castillo.altura())
}

test "alturaTipa" {
	assert.equals(8, tipa.altura())
}

test "auroraEstaViva" {
	assert.that(aurora, estaViva())
}
